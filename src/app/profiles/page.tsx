import { ProfileList } from "@/features/profiles/components/ProfileList";

const ProfilesPage = () => {
	return (
		<main className="relative flex flex-col w-full my-auto">
			<ProfileList />
		</main>
	);
};

export default ProfilesPage;
