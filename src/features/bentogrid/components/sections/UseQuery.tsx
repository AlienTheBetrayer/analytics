import { BentoSection } from "@/features/bentogrid/components/parts/BentoSection";

export const UseQuery = () => {
    return (
        <BentoSection
            src="/auth.svg"
            text="useQuery()"
        ></BentoSection>
    );
};
