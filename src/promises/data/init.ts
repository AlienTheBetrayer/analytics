import { PromiseStates } from "@/promises/data/data";
import { PromiseListeners } from "@/promises/data/listeners";

/**
 * promises object
 */
export const promiseStates = new PromiseStates();

/**
 * promises listeners object
 */
export const promiseListeners = new PromiseListeners();
